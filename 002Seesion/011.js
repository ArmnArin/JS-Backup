function sum(a, b) {
  const coombination = a + b;
  return coombination;
}
const x = Number(prompt("enter first number :"));
const y = Number(prompt("enter second number :"));
const result = sum(x, y);
alert(result);
